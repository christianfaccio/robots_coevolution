[NEAT]
fitness_criterion     = max
fitness_threshold     = 50
pop_size              = 256
reset_on_extinction   = False
no_fitness_termination = True

[DefaultGenome]
# node activation options
# Paper uses modified sigmoid: phi(x) = 1/(1+e^(-4.9x))
# NEAT-Python's "sigmoid" is 1/(1+e^(-x)), so we use response to scale: 4.9*x
activation_default      = sigmoid
activation_mutate_rate  = 0.0
activation_options      = sigmoid
enabled_rate_to_true_add = 0.0
enabled_rate_to_false_add = 0.0

# node aggregation options
aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# node bias options
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 0.5
bias_mutate_rate        = 0.7
bias_replace_rate       = 0.1
bias_init_type          = gaussian

# genome compatibility options
# Paper: c1=1.0, c2=1.0, c3=2.0 (NEAT-Python combines c1+c2 into disjoint_coefficient)
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 2.0

# connection add/remove rates
# Paper: link add prob = 0.1, node add prob = 0.01
conn_add_prob           = 0.1
conn_delete_prob        = 0.0

# connection enable options
# Paper: 75% chance gene disabled if disabled in either parent
enabled_default         = True
enabled_mutate_rate     = 0.01

# Paper: recurrent connections are crucial for success (POMDP domain)
feed_forward            = False
initial_connection      = full

# node add/remove rates
node_add_prob           = 0.01
node_delete_prob        = 0.0

# network parameters
num_hidden              = 0
num_inputs              = 12
num_outputs             = 3

# node response options
# Scale to approximate paper's modified sigmoid phi(x) = 1/(1+e^(-4.9x))
response_init_mean      = 4.9
response_init_stdev     = 0.0
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0
response_init_type      = gaussian

# connection weight options
# Paper: 80% chance of weight mutation, 90% perturbed, 10% new random
weight_init_type        = gaussian
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_max_value        = 30
weight_min_value        = -30
weight_mutate_power     = 0.5
weight_mutate_rate      = 0.8
weight_replace_rate     = 0.1

single_structural_mutation = false
structural_mutation_surer = default

[DefaultSpeciesSet]
# Paper: initial delta_t = 3.0
compatibility_threshold = 3.0

[DefaultStagnation]
species_fitness_func = max
# Paper: species >30 generations old not allowed to reproduce
max_stagnation       = 30
species_elitism      = 2

[DefaultReproduction]
# Paper: champion of species >5 members copied unchanged
elitism            = 1
# Paper: 25% offspring from mutation only (75% survival for crossover)
survival_threshold = 0.2
min_species_size = 5
